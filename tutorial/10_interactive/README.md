# Interactive Mode

Run `make run` followed by `make rerun` to see how the generated command log
can be used as input to rerun the exact scenario. Try modifying the command log
and see how it changes what Whisper does.
