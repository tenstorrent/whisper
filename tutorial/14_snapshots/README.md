# Snapshots

This example demonstrates how to save and load snapshots for a simple program.
Run `make run` to run the program on Whisper, generating a snapshot every 1000
instructions. Run `make load` to use one of the checkpoints to resume the
simulator from where it created that checkpoint.
